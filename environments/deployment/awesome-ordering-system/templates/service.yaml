apiVersion: v1
kind: Service
metadata:
  name: {{ include "awesome-ordering-system.fullname" . }}
  labels:
    {{- include "awesome-ordering-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "awesome-ordering-system.selectorLabels" . | nindent 4 }}
